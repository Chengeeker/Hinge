# Hinge v1.0.32-dev.1

这是 Hinge 的双端开发版，安装包运行版本为 Android `1.0.32+33`、Windows `1.0.32.0`。

## 本版内容

- QQ/微信通知点击只唤醒已运行客户端的主窗口，不再枚举或操作子窗口，不再调用深链和 Android 原通知入口，降低 QQ 卡死风险；
- 客户端已运行时不会重复启动第二个进程；未运行时仍会启动已安装客户端；
- Windows 点击 QQ/微信历史通知时优先恢复已运行的客户端窗口，避免再次启动客户端导致新的登录界面；
- Android 与 Windows 对历史信任设备增加带标记的自动重连、失败后的延迟重试，更新或短暂断线后设备重新上线可自动恢复会话；
- 新设备仍需用户手动连接，自动重连只接受持久化设备身份和信任记录匹配的历史设备；
- Android 增加可选短信/彩信同步，明确授权后监听新消息并通过可信会话转发到 Windows；
- Windows 短信/彩信使用托盘气泡，识别为验证码时点击整条气泡复制，普通通知不会复制；
- Android 新增“工作区 > 通知历史”，按时间记录应用通知并支持应用筛选；
- Windows 侧边栏新增“手机历史通知”，首屏分页读取，支持时间顺序、应用筛选和微信/QQ 原通知入口回跳；
- 修复 Android 在蜂窝网络与 Wi‑Fi 并存时局域网发现包走错网络的问题；
- 修复 UDP 固定端口被占用时临时端口未同步给对端的问题，兼容旧版 52830 默认端口；
- 修复发现成功后反向连接仍使用固定发现端口的问题，并加强 Android 网络服务启动和回前台重绑；
- 修复 Windows 点击 QQ/微信通知时未注册协议触发系统错误对话框的问题，增加自定义盘符、注册表和运行进程探测；
- 保留 ARM64 APK、稳定 Android 签名、自包含 EXE 安装器和便携 ZIP 的发布约定；
- Android 发布包统一输出到 `publish/Hinge.apk`，Windows 资产继续输出到 `publish/windows/`；签名库只从本机/发布者提供的位置读取，签名密码不写入仓库；
- 同步开发文档、协议说明和当前版本的真实设备验收边界。

## 验证

- Android Flutter tests：54 项通过；
- Windows .NET tests：63 项通过；
- Android APK 包名、`versionCode`、ARM64 架构和 Windows 文件版本均已校验；
- 本次 APK 使用本机发布配置完成稳定签名校验；签名库、别名和密码未写入仓库、日志或发行说明；
- Windows 发布资产仅包含 EXE 安装器和便携 ZIP，不提供 MSIX。

## 已知边界

这是开发版。通知历史需要 Android 系统通知访问权限和用户主动开启；Windows 点击微信/QQ历史通知只负责唤醒或启动对应桌面客户端，不承诺定位到具体聊天窗口。不同 Android 厂商短信权限、后台策略、弱网、多网卡和 Windows 防火墙场景仍需真实设备验收。
