# Hinge v1.0.38

这是 Hinge 的双端稳定版本，安装包运行版本为 Android `1.0.38+39`、Windows `1.0.38.0`。

## 本版内容

- 修复 QQ/微信隐藏到托盘时误显示内部 Chromium 窗口，导致出现没有任务栏入口且无法交互的假窗口；
- 隐藏状态改为查询客户端自己的 Electron 托盘宿主和真实通知图标 ID，再发送与用户单击托盘图标等价的回调，由客户端自行恢复真实界面；
- 过滤无标题渲染窗口和托盘宿主，只对客户端已经自行显示的带标题顶层窗口执行前台激活。

本版不重复列出 1.0.37 已包含的安装更新进程处理、重复启动防护和窗口激活链路调整。

## 合规说明

- 补充 Electron MIT 公开实现的兼容参考记录；Hinge 没有复制或打包 Electron 源码，也没有新增 Electron 运行时依赖。

## 验证

- Android Flutter tests：54 项通过；
- Windows .NET tests：63 项通过；
- Android APK 包名、`versionCode`、ARM64 架构和 Windows 文件版本均已校验；
- 本次 APK 使用本机发布配置完成稳定签名校验；签名库、别名和密码未写入仓库、日志或发行说明；
- Windows 发布资产仅包含 EXE 安装器和便携 ZIP，不提供 MSIX。

## 已知边界

通知历史需要 Android 系统通知访问权限和用户主动开启；Windows 点击微信/QQ历史通知只负责唤醒或启动对应桌面客户端，不承诺定位到具体聊天窗口。不同客户端版本、后台策略和 Windows 前台窗口权限仍需真实环境验收。
