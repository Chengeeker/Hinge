<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <PropertyGroup Condition="'$(HingeTransferDiagnostics)' == 'true'">
    <DefineConstants>$(DefineConstants);HINGE_TRANSFER_DIAGNOSTICS</DefineConstants>
  </PropertyGroup>

</Project>
